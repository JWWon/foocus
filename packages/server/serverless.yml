service: server

provider:
  name: aws
  runtime: nodejs14.x
  region: ap-northeast-2

plugins:
  - serverless-offline
  - serverless-plugin-typescript

functions:
  hello:
    handler: src/functions/hello.handler
    events: 
      - http:
          method: get
          path: hello